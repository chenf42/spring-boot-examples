spring:
  datasource:
    url: jdbc:sqlite::resource:mydb.sqlite
    username:
    password:
    driver-class-name: org.sqlite.JDBC
mybatis:
  config-location: classpath:mybatis/mybatis-config.xml
  mapper-locations: classpath:mybatis/mapper/*.xml
  type-aliases-package: com.metaobject.springbootmybatisxml.model
server:
  port: 3000